# transcendence_backend
i eat people YUM YUM YUM


read the go section of this or kys
https://docs.42angouleme.fr/en/Techs/Applications

then do `source ~/.zshrc`
only then can you run the backend with go run ./cmd/api/main.go

//docker run --name space-draw-db -e POSTGRES_USER=user -e POSTGRES_PASSWORD=pass -e POSTGRES_DB=gamedb -p 5432:5432 -d postgres
