storage "file" {
	path = "/vault/file"
}
listener "tcp" {
	address = "0.0.0.0:{{ vault_port }}"
	tls_disable =  1
}

seal "awskms" {
	region = "{{ aws_region }}"
	kms_key_id = "{{ kms_key_id }}"
}

ui = true
disable_mlock = true
