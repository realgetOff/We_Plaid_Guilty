apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: vault-issuer
spec:
  vault:
    server: http://vault.vault.svc.cluster.local:8200
    path: pki/sign/prometheus-pki
    auth:
      tokenSecretRef:
        name: vault-token
        key: token
        namespace: cert-manager
